{
  "name": "embed",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --port 3002",
    "build": "vite build",
    "build:deploy": "vite build && node scripts/build-and-deploy.js",
    "copy:widget": "node scripts/build-and-deploy.js"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "@workspace/eslint-config": "workspace:^",
    "@workspace/typescript-config": "workspace:*",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2",
    "vite": "^5.0.8"
  }
}
